apiVersion: v1
kind: Pod
metadata:
  name: mpi-pod-SN04
  namespace: mpi-mslee
  labels:
     app: mpi-headless-service
spec:
  hostname: mpi-sn04
  subdomain: mpi-service
  nodeSelector:
    worker: "node4"
  containers:
  - name: container
    image: msleedockerhub/k8s_mpi
    ports:
    - containerPort: 8080
